[project]
name = "spa-crawler"
version = "0.1.0"
description = "A Playwright-based crawler for React SPAs"
requires-python = ">=3.9"
dependencies = [
  "playwright>=1.45.0",
  "tqdm>=4.66.0",
  "beautifulsoup4>=4.12.0",
]

[project.scripts]
spa-crawler = "spa_crawler.cli:main"
